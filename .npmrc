registry=https://registry.npmjs.org/
min-release-age=3
# Vercel sets NODE_ENV=production during install, which would omit Angular
# and other build-time packages from devDependencies.
omit=
production=false
