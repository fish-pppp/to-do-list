{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "NODE_OPTIONS=--max-old-space-size=6144 npm_config_production=false NPM_CONFIG_INCLUDE=dev npm run env && node ./tools/git-version.js && npm run sync-core:build && npm run sync-providers:build && npm run shared-schema:build && npm run plugin-api:build && ng build --configuration productionWeb",
  "outputDirectory": "dist/browser",
  "installCommand": "HUSKY=0 NODE_ENV=development npm ci --include=dev --ignore-scripts && npx --yes patch-package@8.0.1 && npx ts-patch install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/index.html",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    },
    {
      "source": "/ngsw.json",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache"
        }
      ]
    },
    {
      "source": "/manifest.json",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache"
        }
      ]
    }
  ]
}
